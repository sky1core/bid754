
//go:build !cgo || !bid754_native || !bid754_decnumber_diff

package bid754

import "testing"

// The decNumber differential gate needs cgo, the pinned Intel BID C
// library, the pinned IBM decNumber 3.68 library, and the
// bid754_decnumber_diff build tag; every other build compiles this stub so
// the gate's absence is a visible skip, never a silent green.

func TestGeneratedDecnumberDifferentialCorpusContract(t *testing.T) {
	t.Skip("decNumber differential gate requires cgo, bid754_native, and bid754_decnumber_diff")
}

func TestGeneratedDecnumberDifferentialRoutingSentinels(t *testing.T) {
	t.Skip("decNumber differential gate requires cgo, bid754_native, and bid754_decnumber_diff")
}

func TestGeneratedDecnumberDifferentialStructured(t *testing.T) {
	t.Skip("decNumber differential gate requires cgo, bid754_native, and bid754_decnumber_diff")
}

func TestGeneratedDecnumberDifferentialDeterministicRandom(t *testing.T) {
	t.Skip("decNumber differential gate requires cgo, bid754_native, and bid754_decnumber_diff")
}
