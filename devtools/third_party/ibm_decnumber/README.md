# IBM decNumber

This directory is a local setup location for the pinned IBM decNumber source
archive and build outputs used by current-tree native verification helpers.

Do not commit downloaded source archives, extracted source trees, object files,
or built libraries from this directory. Use the scripted setup flow documented
in `DEPENDENCIES_SPEC.md` and `README.md`.
