// Code generated by go2rs from flag_operations.go; DO NOT EDIT.

// Mechanical Go/C-shaped port of bid754-go/internal/bidgo/flag_operations.go; the style/shape
// lints below are faithful artifacts of the upstream operand/cast/control-flow
// shape whose behavior is pinned by the readtest/decTest/public-API parity
// verification domains, so they are allowed here rather than rewritten.
#![allow(
    clippy::assign_op_pattern,
    clippy::collapsible_if,
    clippy::comparison_to_empty,
    clippy::double_parens,
    clippy::identity_op,
    clippy::if_same_then_else,
    clippy::manual_clamp,
    clippy::manual_is_multiple_of,
    clippy::manual_range_contains,
    clippy::manual_swap,
    clippy::needless_borrow,
    clippy::needless_else,
    clippy::needless_ifs,
    clippy::needless_return,
    clippy::needless_update,
    clippy::nonminimal_bool,
    clippy::redundant_static_lifetimes,
    clippy::too_many_arguments,
    clippy::unnecessary_cast,
)]

use super::prelude::*;

pub fn bid_signal_exception(mut flagsmask: u32, pfpsf: &mut u32) {
    (*pfpsf) = ((*pfpsf) | (flagsmask & 61));
}

pub fn bid_lower_flags(mut flagsmask: u32, pfpsf: &mut u32) {
    (*pfpsf) = ((*pfpsf) & (!(flagsmask & 61)));
}

pub fn bid_test_flags(mut flagsmask: u32, pfpsf: &mut u32) -> u32 {
    let mut raised: u32 = 0;
    raised = ((*pfpsf) & (flagsmask & 61));
    return raised;
}

pub fn bid_test_saved_flags(mut savedflags: u32, mut flagsmask: u32) -> u32 {
    let mut raised: u32 = 0;
    raised = (savedflags & (flagsmask & 61));
    return raised;
}

pub fn bid_restore_flags(mut flagsvalues: u32, mut flagsmask: u32, pfpsf: &mut u32) {
    (*pfpsf) = ((*pfpsf) & (!(flagsmask & 61)));
    (*pfpsf) = ((*pfpsf) | ((flagsvalues & (flagsmask & 61))));
}

pub fn bid_save_flags(mut flagsmask: u32, pfpsf: &mut u32) -> u32 {
    let mut flagsvalues: u32 = 0;
    flagsvalues = ((*pfpsf) & (flagsmask & 61));
    return flagsvalues;
}

pub fn bid_get_decimal_rounding_direction(mut rnd_mode: u32) -> u32 {
    return rnd_mode;
}

pub fn bid_set_decimal_rounding_direction(mut rounding_mode: u32, mut rnd_mode: u32) -> u32 {
    if (((((rounding_mode == 0) || (rounding_mode == 1)) || (rounding_mode == 2)) || (rounding_mode == 3)) || (rounding_mode == 4)) {
        return rounding_mode;
    }
    return rnd_mode;
}
